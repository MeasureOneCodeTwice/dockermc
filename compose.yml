services:
  test-server:
    build:
      context: src/mcserver
    ports: 
      - "12345:25565"
      - "10000:25500"
    volumes:
      - serverfiles:/serverfiles
    environment:
      SERVER_DOWNLOAD_URL: https://piston-data.mojang.com/v1/objects/95495a7f485eedd84ce928cef5e223b757d2f764/server.jar
    restart: always
    stdin_open: true
    tty: true

volumes:
  serverfiles:
